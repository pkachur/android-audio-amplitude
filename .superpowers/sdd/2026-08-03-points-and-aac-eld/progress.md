# SDD ledger — plan: docs/superpowers/plans/2026-08-03-points-and-aac-eld.md

Ветка: feature/points-aac-eld
База: 9a57827 (план и спека закоммичены)
Базовая линия до работ: test_envelope 13/13, run_tests.py — все проходят.

Этот файл и отчёты рядом с ним добавлены в гит принудительно (каталог
`.superpowers/sdd/` сам себя игнорирует): работа продолжается на другой машине,
и журнал прогона должен переехать вместе с кодом.

## Состояние задач

| Задача | Статус | Коммит |
|---|---|---|
| 1. Сниффер вынесен в `src/sniff.h` | готова, ревью принято | `9722276` |
| 2. TS/ADTS/LATM не уходят в minimp3 | готова, ревью принято | `aac33a4` + правка не закоммичена |
| 3. Целая сумма квадратов в `run()` | реализована, ревью не проводилось | `8c8bedd` |
| 4. `runPoints` — ровно N точек | реализована, ревью не проводилось | `94cd0c1` |
| 5. CLI `--points` | реализована, ревью не проводилось | `7c3b1e6` |
| 6. JNI и Kotlin `points` | реализована, ревью не проводилось | `cbc824a` |
| 7. `check_eld.py` и документация | реализована, ревью не проводилось | `2cdd746` |

## Хроника

Task 1: complete (commits bc53c4b..9722276, review clean)
Task 1: minor (deferred): неиспользуемый #include <cstring> в decoder_open.cpp:7
Task 1: minor (deferred): make unit не прогонялся — GNU make на хосте нет;
  прогнать на Linux/CI до мержа
Task 1: minor -> передан в задачу 2: предупреждения C4996 (fopen) под MSVC /W4
  нарушали глобальный констрейнт «ноль предупреждений». Вылечено в задаче 2
  через /D_CRT_SECURE_NO_WARNINGS в build_host.ps1 (переносимый fopen не трогали —
  предупреждение нестандартное, только MSVC).

Task 2: implemented, committed aac33a4 — РЕВЬЮ НЕ ПРОВЕДЕНО.
  Ревьюер был запущен, но упал с API-ошибкой в середине ответа. Вердикта нет.
  Отчёт исполнителя: task-2-report.md (Status: DONE, без замечаний;
  RED/GREEN зафиксированы, 19/19 test_sniff, 13/13 test_envelope,
  функциональные — все прошли, предупреждений C4996 больше нет).
  Проверено контроллером независимо на момент остановки: сборка и все тесты
  зелёные.

=== ПЕРЕЕЗД НА НОВУЮ МАШИНУ (2026-08-05), работа продолжена ===

Task 2: ревью проведено заново, вердикт — ПРИНЯТО с одним минором.
  Все пять названных рисков проверены и чисты:
  - границы буфера: цикл `i + 3 < lim` гарантирует 4 байта, а
    `validMpegFrameHeader` читает только три (`h[0..2]`); тест 15 закрывает случай;
  - ложных отказов на настоящем MP3 нет: для всех сочетаний версия × слой
    `d[1] & 0xF6` даёт 0xF2/0xF4/0xF6/0xE2, ни одно не равно 0xF0, так что
    `looksLikeAdts` MP3 не перехватывает; отказ «свободного» битрейта осознан;
    и ошибка сниффера в любом случае стоит одной лишней попытки открытия —
    в `decoder_open.cpp` остался откат;
  - обе замены головы 16 -> 1024 на месте (`openFile`, `openFd`);
  - 11 исходных проверок в `test_sniff.cpp` не тронуты (в диффе только добавления);
  - временные файлы убираются: `expect_fail` не бросает исключение.

Task 2: минор ИСПРАВЛЕН — `openFd` читал голову на 1024 байта без оглядки на
  `size`. Дескриптор с парой offset/size — это ассет в APK или срез от
  ContentResolver; на срезе короче 1024 байт в голову попадали байты соседнего
  файла и формат определялся по ним. Чтение зажато по `size`. Проверено
  компиляцией под все четыре ABI (под MSVC этот блок не собирается вовсе —
  он под `#ifdef AMP_HAVE_POSIX_IO`).

Окружение на новой машине (Windows 11, E:\Projects\Amplitude):
  - тулчейны лежат в `E:\Projects\Toolchains`: VS Build Tools 17.14.37 с MSVC
    14.44.35207 в `VSBuildTools\`, NDK r28c (clang 19.0.1) в `AndroidNDK\`;
  - фикстуры сделаны из синтетического источника (ffmpeg из `imageio-ffmpeg`):
    стерео 6 с, каналы 440 и 277 Гц с разной амплитудной модуляцией. Личные
    записи для этого не нужны — `run_tests.py` пересчитывает ожидаемое сам;
  - `audio/` с семью записями перенесён и лежит на месте (каталог в .gitignore).

Починки инфраструктуры сборки, без которых на этой машине ничего не шло
(все три — не по плану, но иначе не собрать и не прогнать):
  - `.ps1` были в UTF-8 без BOM, а Windows PowerShell 5.1 без BOM читает
    скрипты в ANSI: кириллица превращалась в кашу и `build_android.ps1` не
    разбирался вовсе. Добавлен BOM;
  - `build_host.ps1` звал `python3`, а на Windows это заглушка Microsoft Store:
    она есть в PATH, печатает «Python» и возвращает 9009. Теперь берётся первый
    интерпретатор, который реально отвечает на `--version`, и перенаправление
    делает `cmd` (гасить stderr средствами PowerShell нельзя — при
    `$ErrorActionPreference = 'Stop'` вывод заглушки становится фатальной ошибкой);
  - под MSVC `/W4` сторонний minimp3 давал три десятка C4244/C4267/C4456, что
    нарушало глобальный констрейнт «ноль предупреждений». Включение обёрнуто в
    `#pragma warning(push, 0)` — свой код по-прежнему на `/W4`.

Базовая линия на новой машине подтверждена полностью:
  test_envelope 13/13, test_sniff 19/19, функциональные 31/31 — все прошли,
  предупреждений нет ни у MSVC 14.44 `/W4`, ни у clang из NDK r28c
  (`-Wall -Wextra`, все четыре ABI, и бинарник, и `libamplitude.so`).

## Как поднять окружение на новой машине

```sh
git clone https://github.com/pkachur/android-audio-amplitude.git
cd android-audio-amplitude
git checkout feature/points-aac-eld
```

Дальше нужно доложить три вещи, которых в репозитории нет намеренно.

**1. Заголовки minimp3** (`src/minimp3.h`, `src/minimp3_ex.h`) — сторонние,
public domain, не вендорятся:

```sh
make deps          # если есть GNU make
# либо вручную:
curl -fsSL -o src/minimp3.h    https://raw.githubusercontent.com/lieff/minimp3/master/minimp3.h
curl -fsSL -o src/minimp3_ex.h https://raw.githubusercontent.com/lieff/minimp3/master/minimp3_ex.h
```

**2. Тестовые фикстуры** (`tests/test.mp3`, `tests/ref_stereo.pcm`,
`tests/test_mono.mp3`, `tests/ref_mono.pcm`, `tests/test_loud.mp3`) — делаются
одной командой из **любого** аудиофайла, который понимает ffmpeg. Нужен ffmpeg
в PATH либо `pip install imageio-ffmpeg`:

```sh
python3 tests/make_fixtures.py любой_файл.m4a
```

Осмысленный тест получается на записи хотя бы в несколько секунд с меняющейся
громкостью. Конкретные значения фикстур не важны: `run_tests.py` независимо
пересчитывает ожидаемое на Python и сверяет с выходом программы.

**3. Записи для задачи 7** (`audio/*.m4a`) — личные голосовые записи, в
публичный репозиторий сознательно не кладутся. Перенести флешкой или облаком.
**Задачам 2-6 они не нужны вообще** — понадобятся только на задаче 7
(`tests/check_eld.py`).

### Сборка и тесты

На **Windows** (как было здесь): GNU make и clang отсутствуют, всё идёт через
MSVC 14.44 из Visual Studio 2022 Build Tools.

```powershell
cd <репозиторий>; .\build_host.ps1 -Tests   # только юнит-тесты
cd <репозиторий>; .\build_host.ps1 -Func    # + функциональные (нужен python3)
```

`build_host.ps1` сам находит `vcvars64.bat` через `vswhere` и подкладывает
каталог установщика в PATH — без этого `vcvars64.bat` не находит `vswhere`.

На **Linux, macOS или в Termux** работает штатный Makefile, и его наконец можно
прогнать по-настоящему (здесь это не удавалось — make не установлен):

```sh
make MEDIANDK=0        # десктопная сборка, только MP3
make unit              # оба набора юнит-тестов
make MEDIANDK=0 test   # юнит + функциональные
```

Прогон `make unit` на новой машине закрывает отложенный минор задачи 1.

Кросс-сборка под Android (нужна на задаче 6):

```powershell
.\build_android.ps1 -Ndk <путь к android-ndk>
```

## Финальная проверка ветки (2026-08-05)

Сквозные прогоны на новой машине после всех семи задач:
  test_envelope 25/25, test_sniff 19/19, функциональные 46/46, check_eld 7/7.
  MSVC 14.44 `/W4` и clang из NDK r28c на четырёх ABI — без предупреждений.

Независимые проверки сверх набора тестов:
  - `--points 1 -r rms` = 7047 совпало с `--block 264600 -r rms` = 7047. Это два
    совершенно разных пути: слияние подокон против одного окна в run(). Точное
    совпадение подтверждает арифметику слияния лучше, чем реплика на Python
    (та повторяет алгоритм и потому не независима);
  - `--points 3 --raw` даёт ровно 12 байт;
  - `--abs` вместе с `--points` ничего не меняет — свёртки и так неотрицательны.

Найдено при финальной проверке и задокументировано (не дефект, свойство дизайна):
  ширины точек под `--points` не строго одинаковы. Точка набирается из целых
  подокон, а их на точку приходится от 16 до 32, поэтому соседние точки могут
  отличаться шириной примерно на 5%:
      --points 6 : 7373 6401 7280 7421 6398 7364
      --ms 1000  : 7347 6405 7347 7347 6405 7347
  Для рисования волны незаметно, для пересчёта номера точки во время — важно.
  Написано прямым текстом в README и INTEGRATION. Если понадобится ровнее —
  поднять множитель подокон в `runPoints` (сейчас `want * 32`).

Мелочи, оставленные как есть:
  - `block = totalSamples / points` в CLI и JNI усечётся, если точек мало, а
    отсчётов больше 2^31 (это ~13 часов при 44.1 кГц с `--points 1`). Значение
    справочное, на данные не влияет;
  - отложенный минор задачи 1 (неиспользуемый `#include <cstring>` в
    `decoder_open.cpp`) так и не убран.

## Точка возобновления

Все семь задач плана выполнены и закоммичены. Ветка готова к слиянию —
`superpowers:finishing-a-development-branch`, решение за пользователем.

Что остаётся непроверенным (не замалчивая):

1. **AAC ELD на настоящем файле.** Все шесть целых записей в `audio/` — HE-AAC v1
   (`audioObjectType = 5`), а не ELD (`39`). Путь декодирования у них общий, и
   маршрутизация контейнеров проверена, но сам профиль подтверждается только
   кодом. Нужна запись, сделанная `c2.android.aac.encoder` в профиле ELD.
2. **Контейнеры `.3gp`, `.aac`, `.ts` на настоящих файлах.** Сниффер на них
   проверен синтетическими заголовками; сквозного декодирования не было.
3. **Работа на устройстве.** `tests/check_eld.py --device` написан, но телефона
   по adb здесь нет. На десктопе MediaCodec недоступен в принципе, поэтому весь
   AAC-путь проверен только компиляцией и маршрутизацией.
4. **Kotlin-обёртка** компилируется только в проекте приложения: Gradle нет,
   сверка сигнатур с JNI сделана глазами.

## Решения, принятые по ходу (не выводятся из кода)

- Предполётная сверка плана нашла два дословных повтора логики; вместо копий
  заведены `amp::mixFrame` в `envelope.h` и `writeHeaderPrefix` в
  `amplitude.cpp`. План поправлен коммитом `bc53c4b`.
- Настоящего AAC ELD среди записей нет: все шесть целых файлов — HE-AAC v1
  (`audioObjectType = 5`), а не ELD (`39`). Записано открытым пунктом в спеке.
  Седьмой файл (65536 байт) — оборванная запись без `moov`, годится как
  негативная фикстура.
- Аудио и фикстуры не кладутся в репозиторий: он публичный, а записи личные.
